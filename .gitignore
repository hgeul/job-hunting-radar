# ── 개인정보 (공개 리포에 절대 커밋 금지) ────────────────────
# 실제 지원자 설정·이력·이력서 PDF·매칭 결과. 예시 템플릿(*.example.*)만 공개.
config.json
config.*.json
!config.example.json
profile.md
profile.*.md
!profile.example.md
# 감시 대상 기업 목록(어느 회사를 노리는지 = 개인정보). 공개는 example 만.
target-companies.json
target-companies.*.json
target-companies.yaml
target-companies.*.yaml
target-companies.yml
target-companies.*.yml
!target-companies.example.json
!target-companies.example.yaml
!target-companies.example.yml
정미/
resumes/
matches/
# AI 하네스·작업 문서. 로컬 전용(지원서·구직 진행상황 등 개인정보 포함).
# .git/info/exclude 에도 있지만 그건 버전관리·클론 전파가 안 된다. 여기 한 번 더 건다.
/docs/
/.claude/
/CLAUDE.md
# 런타임 상태 캐시(본 공고 id·createdAt)
state/

# ── 파이썬 / 가상환경 ────────────────────────────────────────
.venv/
venv/
__pycache__/
*.pyc

# ── 비밀·환경변수 ────────────────────────────────────────────
.env
*.key

# ── OS/에디터 ────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/

# ── Windows/cygwin 크래시 덤프 ──────────────────────────────
*.stackdump

# ── 리팩터 회귀 baseline(로컬 전용, 크롤 대상 데이터 포함) ──
# .local/ = 스냅샷 하네스·fixture. _old_*.py = 리팩터 전/후 A/B 비교용 사본.
.local/
_old_*.py
